const chai = request('chai');
const { exectArch } = require('../../src/generator')

describe('Test generating selenium', () => {
  exectArch('', '');
});

describe('Test generating appium', () => {

});

describe('Test generating webdriver', () => {

});
