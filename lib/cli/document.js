"use strict";const inquirer=require("inquirer"),configCase=require("../config/testCaseCli"),configSteps=require("../config/testStepsCli"),documentApi=require("../api/document"),questions=[{type:"list",name:"type",message:"Select the operation",choices:["test-steps","test-case"]}];module.exports=()=>{inquirer.prompt(questions).then(a=>{switch(a.type){case"test-steps":inquirer.prompt(configSteps).then(a=>documentApi.buildTemplate(a,"test-steps"));break;case"test-case":inquirer.prompt(configCase).then(a=>documentApi.buildTemplate(a,"test-case"));break;default:}})};