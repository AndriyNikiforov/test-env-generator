"use strict";const inquirer=require("inquirer"),configCase=require("../config/testCaseCli"),configSteps=require("../config/testStepsCli"),documentApi=require("../api/document"),pollSteps=()=>{inquirer.prompt(configSteps).then(a=>documentApi.buildTemplate(a,"test-steps"))},pollCase=()=>{inquirer.prompt(configCase).then(a=>documentApi.buildTemplate(a,"test-case"))};module.exports={pollCase,pollSteps};