"use strict";const inquirer=require("inquirer"),configCase=require("../config/testCaseCli"),configSteps=require("../config/testStepsCli"),documentApi=require("../api/document");module.exports={pollCase:()=>{inquirer.prompt(configSteps).then(a=>documentApi.buildTemplate(a,"test-steps"))},pollSteps:()=>{inquirer.prompt(configCase).then(a=>documentApi.buildTemplate(a,"test-case"))}};