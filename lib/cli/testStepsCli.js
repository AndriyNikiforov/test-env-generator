"use strict";const inquirer=require("inquirer"),{log}=require("console"),config=require("../config/testStepsCli"),documentApi=require("../api/document"),poll=()=>{log("Welcome to test steps generator"),inquirer.prompt(config).then(a=>documentApi.buildTemplate(a,"test_steps"))};module.exports=poll;