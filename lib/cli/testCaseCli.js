"use strict";const inquirer=require("inquirer"),{log}=require("console"),config=require("../config/testCaseCli"),documentApi=require("../api/testCase"),poll=()=>{log("Welcome to test case generator"),inquirer.prompt(config).then(a=>documentApi.buildTemplate(a))};module.exports=poll;